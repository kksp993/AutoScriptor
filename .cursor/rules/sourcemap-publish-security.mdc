---
description: npm/前端发布勿附带可还原源码的 source map；构建产物检查清单
globs: webapp/**
alwaysApply: false
---

# Source map 与发布安全

## 为何重要

公开 npm 包或安装包中若包含未剥离的 `*.map`（尤其是带 `sourcesContent` 的完整 source map），可能**直接还原仓库内大量源码**，包括业务逻辑、系统提示词与工具调用设计。这不是「被黑」，而是**构建/发布配置失误**。

## 发布前检查

- 确认生产构建**不输出**可公开分发的 `.map`，或从发布物中**排除** `**/*.map`（例如 `files` 字段、`npmignore`、electron-builder `extraResources`/`asarUnpack` 等路径中不要误拷 `dist/**/*.map`）。
- 若必须保留 map 仅用于自建崩溃上报，应**仅限内网或私有符号服务**，且勿打进对外 tarball。
- CI/发布脚本里可对产物做一次扫描：`find`/`Get-ChildItem` 列出待发布目录中的 `*.map` 并失败退出。

## 与本仓库

- 维护 `webapp/`、Electron 或任何会 `npm pack` / `npm publish` 的流程时，将上述检查纳入 `scripts/build_release.py` 或对应流水线。

## 参考：类 Claude Code 的 CLI 代理形态（设计备忘）

若实现「终端里的智能代理」类工具，常见结构是：**React + Ink** 做 TUI、**REPL 主循环**、自然语言与 **`/` 斜杠命令** 并存、通过**工具层**调用 LLM API。本段仅作架构参考，不约束本仓库实现。
