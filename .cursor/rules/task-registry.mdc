---
description: TaskRegistry 任务注册解耦：cfg["tasks"]只存用户配置，fn/order/param_meta存TaskRegistry。操作任务注册、调度、执行相关模块前必读。
globs: "**/*.py"
alwaysApply: false
---

# TaskRegistry 任务注册与 cfg 解耦

任务的**运行时数据**（fn、order、param_meta）存于 `TaskRegistry` 单例，**用户配置**（on、next_exec_time、params）存于 `cfg["tasks"]`。两者通过 slash 路径关联（如 `"每日任务/村庄/宠物培养"`）。

## 接口速查

```python
from AutoScriptor.utils.task_registry import task_registry

task_registry.get_fn(path)          # 取任务函数
task_registry.get_order(path)       # 取注册顺序
task_registry.get_param_meta(path)  # 取枚举参数元数据
task_registry.has_task(path)        # 判断是否已注册
task_registry.set_fn(path, fn)      # 替换函数（测试用）
task_registry.register(path, fn, order, param_meta)  # 注册（仅 @register_task 调用）
```

## 关键约定

- **不要**在 `cfg["tasks"]` 节点中读写 `fn`、`order`、`param_meta`，这些只在 TaskRegistry
- **叶子判断**：`TaskTree.is_leaf(node)` 等价于 `'on' in node`，不再检查 `fn`
- **到期收集**：`Scheduler._collect_due` 额外调 `task_registry.has_task(path)` 过滤残留配置
- **测试 mock**：替换任务函数用 `task_registry.set_fn(path, new_fn)`，不改 cfg
- **重载**：`force_reload_tasks()` 会先 `task_registry.clear()` 再重新注册

## 详细文档

`docs/AutoScriptor/refactor/task_registry_decouple.md` — 完整 API、数据流、受影响模块清单

## 测试

```powershell
python -m unittest discover -s test/test_task_registry -v
```
