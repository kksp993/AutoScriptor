---
description: 重构技术文档索引：TaskRegistry解耦、VLMTarget视觉定位、RuntimeContext生命周期、Ollama本机VLM部署的接口文档与测试。修改相关模块前必读。
globs: "**/*.py"
alwaysApply: false
---

# 重构技术文档总索引

所有重构接口文档位于 `docs/AutoScriptor/refactor/`，完整索引见 `docs/AutoScriptor/refactor/README.md`。

**修改以下模块前务必先阅读对应文档。**

## 文档目录

| 文档 | 阶段 | 涉及模块 | 核心接口 |
|------|------|----------|----------|
| `task_registry_decouple.md` | 一 | `task_registry.py` `task_register.py` `task_manager.py` `scheduler.py` | `TaskRegistry` 单例、`task_registry.get_fn/has_task` |
| `vlm_target_api.md` | 三 | `targets.py` `rec.py` `api.py` | `VLMTarget` 类、`V()` 工厂函数、`vlm_locate()` |
| `runtime_context_api.md` | 四 | `runtime_context.py` `scheduler.py` `run.py` | `RuntimeContext.init/refresh/shutdown` |
| `ollama_deploy.md` | 三 | `config.json` `vlm/config.py` `tools/setup_ollama.py` | `llm.url` `llm.model` `llm.use_agent` |

## 关键约定

- `task_registry` 存运行时数据（fn/order），`cfg["tasks"]` 只存用户配置（on/params）；详见 `task-registry.mdc`
- `V("描述", box=Box(...))` 是 VLM 定位的唯一入口，与 `I()`/`T()`/`B()` 平级
- `runtime_ctx` 是 `services.core.runtime_context` 的模块级单例，管理 `mixctrl`/`mumu`/`bg`/`vlm_client` 生命周期
- 不要直接修补 `AutoScriptor.mixctrl` 等全局变量，应通过 `runtime_ctx.refresh()` 刷新
- Ollama 配置在 `config.json` 的 `llm` 字段，`use_agent=false` 时 VLM 功能不激活

## 测试

```powershell
python -m unittest discover -s test/test_task_registry -v   # 阶段一
python -m unittest discover -s test/test_perf_optimize -v   # 阶段二
python -m unittest discover -s test/test_refactor_v3v4 -v   # 阶段三、四
```
