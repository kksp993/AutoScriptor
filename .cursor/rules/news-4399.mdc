---
description: 4399 论坛资讯（News）默认通行证与配置约定。
globs:
  - services/webui/routes/news*.py
  - config template.json
alwaysApply: false
---

# 4399 资讯（News）与通行证

## 默认「后门」账号（可提交仓库）

仓库内 **`config template.json`** 的 **`news.account` / `news.password`** 为项目约定的 **4399 通行证**，专用于 BBS 资讯代理（`/api/news/*`），**非个人敏感账号**，可随模板一并提交。

- 当前默认值：`news.account` = `85rwm3janyyc`，`news.password` = `123456`。

## 代码约定

- **`services/webui/routes/news_4399_session.py`**：`get_news_4399_credentials_from_server()` **优先**读取主配置中的 **`news.*`**，未配置时再回退到 **`game.*`**（与自动化共用）。
- 使用 ptlogin 会话仍须 WebUI **credential_unlock**（安全密码解锁）后才会代拉需登录的论坛页；前端 **`make_public_config`** 会剥离 `account` / `password` 字段，不向前端泄露明文。

## 对 Agent 的提示

- 修改资讯抓取 / 代理逻辑时，勿把 **`news` 默认账密**误当成需轮换的生产密钥；若用户要求覆盖默认值，改 **`config template.json`** 或用户本地 **`config.json`** 中的 **`news`** 段即可。
- **不要**在无关功能中复用该账号做自动化游戏登录，除非用户明确要求。
