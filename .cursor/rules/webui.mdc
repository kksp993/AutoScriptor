---
description: WebUI 架构（FastAPI + Vue）、Electron 壳、调试结束后必须释放端口。
globs:
  - services/webui/**
  - webapp/**
  - gui.py
alwaysApply: false
---

# WebUI 架构（FastAPI + Vue 3 组件）

## 技术栈

- **后端**：FastAPI + uvicorn，WebSocket 日志推送（非 Socket.IO）
- **前端**：Vue 3 CDN + Element Plus + Tailwind，全局组件注册（无构建工具）
- **依赖**：`fastapi`、`uvicorn[standard]`、`websockets`（requirements.txt 中，已移除 flask/eventlet）

## 目录结构

```
services/webui/
  server.py                          # FastAPI 应用、所有 /api/* 路由、WebSocket /ws/logs
  static/
    index.html                       # HTML 壳，加载 vendor + 组件 + app.js
    css/style.css                    # 自定义样式
    js/
      app.js                        # Vue 应用实例、共享状态、API 调用、WebSocket
      components/
        Overview.js                  # 总览面板（状态卡片、调度控制、任务队列、日志）
        TaskTree.js                  # 递归任务树（可折叠分组、状态徽章）
        TaskPanel.js                 # 任务编辑面板（树 + 控制按钮 + 日志）
        Settings.js                  # 模拟器/OCR/应用设置
        AboutPanel.js                # 关于页（版权声明；见下文版本号约定）
  vendor/                            # 第三方 CDN 本地镜像（自动下载）
```

## 关键约定

- **API 路由**全部以 `/api/` 为前缀（如 `/api/overview`、`/api/run`、`/api/tasks`）
- **WebSocket**地址：`/ws/logs`，使用原生 `WebSocket` 而非 Socket.IO
- **配置获取**：前端通过 `GET /api/refresh` 拉取配置，不使用 Jinja 模板渲染
- **敏感字段**：`make_public_config()` 在返回前端前清除 `fn`、`encryption`、`account`、`password`
- **组件通信**：props 向下传数据，emits 向上发事件，app.js 统一管理状态
- **默认首页**：`activeTab` 默认值为 `'overview'`（总览面板）

### 版本号与「关于」页（发版必查）

- **Electron 桌面包**的版本号以 **`webapp/package.json`** 的 **`version`** 为准（`electron-builder` 会读取）。
- **`services/webui/static/js/components/AboutPanel.js`** 中的 **`APP_DISPLAY_VERSION`** 必须与 **`webapp/package.json` → `version`** **保持一致**。
- **发新版或 bump 桌面端版本时**：同时修改上述两处，避免安装包/关于页显示的版本不一致。

## 新增/修改组件

1. 组件文件放 `static/js/components/` 下，用全局变量导出（`const MyComponent = {...}`）
2. 在 `index.html` 底部 `<script>` 标签中按依赖顺序加载
3. 在 `app.js` 的 `createApp({ components: {...} })` 中注册

## 测试

```powershell
python -m pytest test/test_refactor_v3v4/test_webui.py -v
```

## 调试结束后释放端口（必做）

在本地启动过 WebUI / Electron 后，**结束本次调试或对话收尾前**，必须协助用户关闭占用端口的进程，避免 `[Errno 10048]` 等端口占用问题。

### 后端（uvicorn / `python gui.py` / `server.py`）

- 默认监听 **`127.0.0.1:5000`**（见 `services/webui/server.py` 中 `run_webui`）。
- **PowerShell** 示例（释放 5000 端口）：

```powershell
$pids = (Get-NetTCPConnection -LocalPort 5000 -ErrorAction SilentlyContinue).OwningProcess | Select-Object -Unique
foreach ($p in $pids) { if ($p -and $p -ne 0) { Stop-Process -Id $p -Force -ErrorAction SilentlyContinue } }
```

- 若 `Get-NetTCPConnection` 不可用，可用：`netstat -ano | findstr ":5000 "` 查 PID，再 `taskkill /PID <pid> /F`。

### 前端（Electron 桌面壳）

- 从 `webapp/` 运行过 `npm start` / `npx electron .` 时，**一并结束 Electron 进程**，否则可能仍占资源或重复拉起 Python 子进程：

```powershell
Get-Process -Name "electron" -ErrorAction SilentlyContinue | Stop-Process -Force
```

### 对 Agent 的约定

1. 若在本次会话中**曾建议或执行**过启动 WebUI / Electron，在收尾时**主动提醒**用户执行上述命令，或在用户同意下代为执行 `run_terminal_cmd` 清理。
2. 不要假设「用户会自己关」；**明确写出**要关的进程类型（Python 占 5000、electron）。
3. 清理后可用 `netstat -ano | findstr ":5000 "` 确认端口已释放。
